TODO:

Future versions

? playground.py is new UI
    ? get ui state working properly
    ? incorporate in AI
    ? incorproate in prompts
    ? incorporate in context / injection
    ? get auth working

Admin:
? settings panel
? settings model
    - ai model
    - system prompt
    - temperature
    - whitelist
? pick a prompt 




v1.0.1

x library to build contexts from ipynb
x library to load contexts in the app
x build dropdown for selecting a context (lab-04, homework-04 etc.)
x inject context into memory
x add context to logging 
x generate a query on behalf of the user I'd like to ask about....

X chat logging
X AI should know your name
x AI should know context (what are we talking about?)
    - how to represent that in the UI????

x Azure auth in deployment
x different AI icons depending on the setup azure ollama, rag, norag

x get this RAG working with the prompts in the chatapp
x same system prompt (see below)
x different user prompt goes from raw to ragbot (see below)
x configurable at runtime gpt 4o-mini or ollama 
x add a few more sources to the vector store
x limit access to roster of students ??
x random assignment by netid. mafudge can pick
x setup vecotor store on storage.cent-su.org
x change logging to include rag=T/F, and ai model

